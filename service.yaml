apiVersion: v1
kind: Service
metadata:
  name: moviereview-service
spec:
  selector:
    app: moviereview
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30084
  type: NodePort
